# === Database ===
spring.datasource.url=jdbc:mariadb://localhost:3306/bookstore?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# DEV i�in �nerilir (PROD'da migration arac? kullan?n)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === Security / JWT ===
app.jwt.secret=change-this-please-very-secret-key-32-bytes-minimum
app.jwt.expire-minutes=120
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB
# src/main/resources/application.properties
ocr.tessdata-path=C:/tessdata
# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.repositories.enabled=false

# Kilit politikası
app.auth.max-fail=5
app.auth.lock-minutes=10
# Yükleme boyutu
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# Yükleme klasörü (değiştirebilirsiniz)
snap.upload-dir=uploads/snaps


# CORS for local dev
app.cors.allowed-origins=http://localhost:8080,http://127.0.0.1:8080,http://localhost:8081,http://127.0.0.1:8081,http://localhost:8000,http://127.0.0.1:8000
